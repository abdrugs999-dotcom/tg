# Telegram Withdraw Bot

A simple Telegram bot with:
- Must join channel verification
- Withdrawal request sending to an admin channel

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
